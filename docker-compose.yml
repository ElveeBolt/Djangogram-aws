version: "3.9"
services:
  web:
    container_name: web
    hostname: web
    build:
      context: ./djangogram
      dockerfile: Dockerfile
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "8000:8000"
    env_file:
      - ./.env.dev